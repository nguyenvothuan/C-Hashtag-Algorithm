const split = (str: String) => {
    const arr = [];
    let curStr = ''
    let inBracket = false;
    let curNum =0
    for(const chr in str) {
        if (!inBracket) {
            
        }
    }
}
var decodeString = function(s: String) {
    
}